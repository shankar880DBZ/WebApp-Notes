<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>File Viewer</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-zinc-900 flex items-center justify-center min-h-screen">
    <div class="bg-zinc-800 shadow-lg p-6 rounded-lg max-w-2xl w-full">
        <a href="/" class="text-blue-500 mb-4 inline-block">⬅ Go Back</a>
        
        <form action="/edit" method="POST">
          <label for="previous" class="text-white font-semibold block mb-2">Previous Title of the Note</label>
          <input
            type="text"
            id="previous"
            name="previous"
            placeholder="Previous Title of the Note"
            class="bg-zinc-800 text-white font-semibold text-md py-2 px-4 rounded-md outline-none w-full block border-2 mb-4"
            required
            value="<%= filename.replace('.txt', '') %>"
            readonly
          />
          
          <label for="new" class="text-white font-semibold block mb-2">New Title of the Note</label>
          <input
            type="text"
            id="new"
            name="new"
            placeholder="New Title of the Note"
            class="bg-zinc-800 text-white font-semibold text-md py-2 px-4 rounded-md outline-none w-full block border-2"
            required
          />
          
          <button
            type="submit"
            class="bg-yellow-600 py-2 px-4 rounded-md mt-4 text-white font-semibold text-md cursor-pointer hover:bg-yellow-800 transition-all duration-500"
          >
            Update Name 
          </button>
        </form>
    </div>
  </body>
</html>